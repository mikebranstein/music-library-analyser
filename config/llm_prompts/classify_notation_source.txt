You are a music librarian assistant with expert score-reading skills. You are shown one or more
rendered page images from a SINGLE instrumental PART PDF in a concert-band library. Look at the
IMAGE(S) only -- do not run OCR, transcribe notes, or search the web.

Part context:
- File name: {pdf_filename}
- Source folder: {piece_folder}

Image file path(s) to open and inspect (absolute paths on this machine):
{image_paths}

Judge the following from the image(s):

1) notation_source -- how the music was produced:
   - "printed_original": professionally engraved/typeset or a clean printed original; uniform
     noteheads, stems, and typeset text.
   - "handwritten": the music was written by hand (freehand noteheads/stems/beams), even if the
     staff lines were pre-printed. Hand-copied manuscript counts as handwritten.
   - "mixed_or_uncertain": genuinely cannot tell, or the page mixes printed and handwritten music.

2) legibility -- can a human musician actually read and play from this page?
   - "good": the notes, rhythms, and markings can be read without effort. Judge legibility ONLY by
     whether the musical content is readable -- NOT by cosmetic condition. A clean, professionally
     engraved print is "good" even if it has a rubber stamp/ownership mark, a fold or scan line,
     minor speckling, or slightly tight (but clearly readable) divisi/chord passages. Neat
     handwriting that reads clearly is also "good".
   - "fair": genuinely degraded so that reading takes real effort in places -- faint/washed-out
     ink, cramped writing with ambiguous pitches or rhythms, or crowding that forces you to guess.
     Reserve this for real readability problems, not mere cosmetic blemishes on an otherwise clear
     page.
   - "poor": hard to read throughout: heavy bleed-through, significant cut-off content, very faint,
     or ambiguous across most of the page.

Do not penalize legibility for mere page skew or rotation, stamps, fold marks, or cosmetic
blemishes if the musical content is otherwise readable.

3) instruments -- read the PRINTED PART LABEL (usually top-centre or upper-left of the first music
   page) and report which instrument(s) this part is written for. Use ONLY tokens from the allowed
   list below, choosing the closest match. A single physical part may cover more than one
   instrument (e.g. a doubling like Flute + Piccolo, or a percussion book that covers several
   instruments). Return an empty list if you genuinely cannot read any instrument name.

4) part_numbers -- if the printed label names specific numbered chairs (e.g. "1st & 2nd Flutes",
   "Oboe 1 & 2", "Clarinet 1-2-3"), return those chair numbers as integers in order ([1, 2]). If
   the label carries no chair number, or names only a single unnumbered part, return an empty list.

5) part_label -- the printed part label transcribed verbatim (e.g. "1st & 2nd Flutes"), or an empty
   string if none is visible.

Report only what the printed label actually shows -- never guess numbers from the file name.

Allowed instrument tokens (choose ONLY from this list):
{allowed_instruments}

Respond with exactly one JSON object between the sentinel lines and nothing else:
<<<VISION_JSON>>>
{"notation_source": "printed_original", "legibility": "good", "instruments": ["flute"], "part_numbers": [1, 2], "part_label": "1st & 2nd Flutes", "confidence": 0.0, "notes": ""}
<<<END_VISION_JSON>>>
